services:
  maxtg_bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maxtg_bot
    env_file:
      - .env
    environment:
      BOT_NAME: maxtg_bot
    restart: unless-stopped
